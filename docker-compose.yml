version: "3"

services:
    client:
        container_name: client
        build:
          context: ./client
        volumes:
          - './client:/app'
          - '/app/node_modules'
        ports:
          - 3000:3000
        environment:
          - CHOKIDAR_USEPOLLING=true
